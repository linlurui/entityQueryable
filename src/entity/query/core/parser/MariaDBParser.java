package entity.query.core.parser;

import entity.query.core.DataSource;

public class MariaDBParser extends MysqlParser {

    public MariaDBParser(DataSource ds) {
        super(ds);
    }
}
